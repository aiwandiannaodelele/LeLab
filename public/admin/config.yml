# yaml-language-server: $schema=https://unpkg.com/@sveltia/cms/schema/sveltia-cms.json

backend:
  name: github
  repo: aiwandiannaodelele/LeLab
  base_url: https://cmsauth.1l.lol

media_folder: public/images
public_folder: /images

locale: zh_Hans

collections:
  - name: posts
    label: 文章
    label_singular: 文章
    folder: content/posts
    create: true
    delete: true
    slug: "{{slug}}"
    preview_path: posts/{{slug}}
    fields:
      - { label: 标题, name: title, widget: string, required: true }
      - { label: 链接标识 (slug), name: slug, widget: string, required: false, hint: "留空自动从标题生成" }
      - { label: 发布日期, name: date, widget: datetime, format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false, required: true }
      - { label: 摘要, name: excerpt, widget: text, required: false }
      - { label: 标签, name: tags, widget: list, required: false }
      - { label: 封面色, name: cover, widget: color, allow_input: true, required: false }
      - { label: 封面图, name: coverImage, widget: image, required: false, hint: "上传图片后自动覆盖封面色" }
      - { label: 正文, name: body, widget: markdown, required: false }